%-----------------------------------------------------------------------------%
include "conference.base.zinc";

% Use the FD/IC/LP backend with the default FD, IC, and LP solvers.
%
backend = backend_fdic(default, default, default);

% search = null;
search = tree_search(sessions, in_order, min_assign);

%-----------------------------------------------------------------------------%
%-----------------------------------------------------------------------------%
