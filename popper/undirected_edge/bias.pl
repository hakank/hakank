max_vars(2).
max_body(2).
max_clauses(2).

% head
head_pred(target,2).
body_pred(edge,2).

% type(target,(node,node)).
% type(edge,(node,node)).
% direction(target,(in,out)).
% direction(edge,(in,out)).
