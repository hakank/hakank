max_vars(6).
max_body(3).
max_clauses(2).

% head
head_pred(target,2).
type(target,(element,element)).
direction(target,(in,out)).

% body
body_pred(target,2).
type(target,(element,element)).
direction(target,(in,out)).

body_pred(edge,2).
type(edge,(element,element)).
direction(edge,(in,out)).
