/*
  Factorial. To check how large the integers can be
*/


fixed Integer N = 10;
distinct Timestep q[10];

Random Integer factorial(Integer f) ~
    if f <= 1 then 1 else factorial(f-1)*f;

// Don't work!
// Random IntegerMatrix factorial_list(Integer n) ~
//    {i for Integer i: i>= 0 & i <= n & q[@i] == factorial(i) };

query factorial(10);